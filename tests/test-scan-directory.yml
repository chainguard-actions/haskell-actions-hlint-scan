name: Test HLint scan on directory
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: push
      GITHUB_REF: refs/heads/main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run HLint scan on fixtures directory
        id: action
        uses: .
        with:
          path: pristine/haskell-actions--hlint-scan/v1.2.0/tests/fixtures
          token: dummy-token-for-act
        continue-on-error: true
      - name: Assert action ran on directory
        run: |
          echo "Action outcome: ${{ steps.action.outcome }}"
          # The action may fail at the SARIF upload step due to dummy token,
          # but it should at least start and run HLint on the directory.
          if [ "${{ steps.action.outcome }}" = "skipped" ]; then
            echo "FAIL: action was skipped unexpectedly"
            exit 1
          fi
          echo "PASS: action ran on directory (outcome: ${{ steps.action.outcome }})"
